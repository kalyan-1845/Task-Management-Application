{
  "name": "vortex-tasks-root",
  "version": "1.0.0",
  "description": "Premium Full-Stack Task Management Workspace Orchestrator",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
